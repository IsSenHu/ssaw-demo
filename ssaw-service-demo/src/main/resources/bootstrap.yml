spring:
  application:
    name: app-demo
  cloud:
    config:
      profile: test
      label: dev-husen
      username: admin
      password: 521428Slyt
      discovery:
        enabled: true                   #从服务中心拉取配置文件
        service-id: SSAW-CONFIG-SERVER
      fail-fast: true                   #失败快速响应
      retry:
        multiplier: 1.2                 #下一次间隔乘数
        initial-interval: 1000          #最初间隔数 单位毫秒
        max-interval: 2000              #最大间隔数
        max-attempts: 6                 #最大重试次数
eureka:
  client:
    service-url:
      defaultZone: http://admin:14780118635@www.senhu.com:10001/eureka/
  instance:
    prefer-ip-address: true

server:
  port: 20001
